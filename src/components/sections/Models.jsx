import React from 'react';
import styles from '../../styles/modules/Models.module.css';

const Models = () => {
  return (
    <section className={styles.section} id="models">
      <h2>Models</h2>
    </section>
  );
};

export default Models;
